import {TranslationRequestPayload} from './types';

export async function translate({word, lang}: TranslationRequestPayload) {
  try {
    // will be filled soon
  } catch (error) {
    throw error;
  }
}
